# MAINTAINER: admodev <admodevcodes@outlook.com>

name: ch-commerce

on:
  push:
    branches: [master, pre-release, test]
  pull_request:
    branches: [master, pre-release, test]

permissions:
  checks: write
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x, 16.x]

    steps:
      - name: Setup cache
        uses: actions/cache@v2
        with:
          path: |
            ~/.npm
            ${{ github.workspace }}/dist/cache
          key: ${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}-${{ hashFiles('**.[jt]s', '**.[jt]sx') }}
          restore-keys: |
            ${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}-

      - name: Upgrade NPM
        run: npm install -g npm
      - uses: actions/checkout@v2

      - name: Using Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies and build app
        run: |
          npm ci --no-optional --legacy-peer-deps
          npm run build --if-present

# TODO!: add test script to last step
